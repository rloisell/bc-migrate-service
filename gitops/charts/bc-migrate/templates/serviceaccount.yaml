# serviceaccount.yaml — bc-migrate-service

{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bc-migrate-service.serviceAccountName" . }}
  labels:
    {{- include "bc-migrate-service.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
