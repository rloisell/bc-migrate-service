apiVersion: v1
kind: Service
metadata:
  name: {{ include "bc-migrate-service.fullname" . }}
  labels:
    {{- include "bc-migrate-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bc-migrate-service.selectorLabels" . | nindent 4 }}
